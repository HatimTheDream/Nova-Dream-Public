{
  "id": "edition3-sources",
  "name": "Nova Dream source transfer",
  "description": "Transfer verified Nova Dream sources into managed native media for an exact conversation.",
  "activation": { "onStartup": true },
  "configSchema": {
    "type": "object", "additionalProperties": false,
    "properties": { "epoch": { "type": "string", "format": "uuid" }, "bundlePath": { "type": "string", "minLength": 1 }, "cacheDirectory": { "type": "string", "minLength": 1 } },
    "required": ["epoch", "bundlePath", "cacheDirectory"]
  }
}
