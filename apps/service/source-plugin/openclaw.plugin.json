{
  "id": "edition3-sources",
  "name": "Edition 3 source transfer",
  "description": "Transfer verified Edition 3 sources into managed native media for an exact conversation.",
  "activation": { "onStartup": true },
  "configSchema": {
    "type": "object", "additionalProperties": false,
    "properties": { "epoch": { "type": "string", "format": "uuid" }, "bundlePath": { "type": "string", "minLength": 1 }, "cacheDirectory": { "type": "string", "minLength": 1 } },
    "required": ["epoch", "bundlePath", "cacheDirectory"]
  }
}
